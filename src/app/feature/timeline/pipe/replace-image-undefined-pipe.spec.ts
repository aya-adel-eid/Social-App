import { ReplaceImageUndefinedPipe } from './replace-image-undefined-pipe';

describe('ReplaceImageUndifinedPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceImageUndefinedPipe();
    expect(pipe).toBeTruthy();
  });
});
